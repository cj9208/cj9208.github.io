# risk factor
- main
  - get_model
  - create_loaders
  - train_epoch(epoch, model, optimizer, train_loader, writer, args)
    - train_loader
      - iter_batch
    - model
      - model.train()
    - loss_fn(pred, label, cap, cnts, index, args, details=False)
      - regression
      - distance(pred, cnts, index)
        - zscore
    - optimizer
    - writer
  - test_epoch(epoch, model, test_loader, writer, args, prefix='Test')
    - test_loader
      - iter_daily
    - model
      - model.eval()
  - inference
  - logic
    - train one epoch, eval one epoch, save the best parameters
    - average_params(params_list)
  - to be figured out
    - index