# RL
- assignment 1
  - def evaluate(policy, num_episodes, seed=0, env_name='FrozenLake8x8-v0', render=False)
  - class TabularRLTrainerAbstract
    - env_name, env, obs_space, actio_space
    - _get_transitions
    - evaluate, render, print_table
    - train
      - need to be implemented
  - policy iteration
    - class PolicyItertaionTrainer(TabularRLTrainerAbstract):
      - gamma, eps
      - train
        - update value function
        - update policy function
    - def policy_iteration(train_config=None)
      - do policy iteration training given config
      - return well-trained agent
  - value iteration
    - class ValueIterationTrainer(PolicyItertaionTrainer)
    - def value_iteration(train_config=None)
  - TD control
    - class SARSATrainer(TabularRLTrainerAbstract)
      - on-policy
      - gamma, eps, learning_rate
      - eps-greedy policy
    - def sarsa(train_config=None)
    - class QLearningTrainer(TabularRLTrainerAbstract)
      - off-policy
    - def q_learning(train_config=None)
  - Monte Carlo Control
    - class MCControlTrainer(TabularRLTrainerAbstract)
      - gamma, eps, max_episode_length
      - returns, q-table
    - def mc_control(train_config=None)
- assignment 2
  - functions
    - evaluate
      - evaluate the given policy and return the mean episode reward
    - run
      - run the trainer and report progress
  - AbstractTrainer
    - preprocess
      - initialize_parameters
      - process_state
    - approximation
      - compute_values
        - compute the value of given state for all potential action
      - compute_action
        - eps-greedy exploration
    - gradient decendent
      - compute_gradient
      - apply_gradient
    - train
      - play the game once
        - record states, actions, rewards
      - do one gradient decendent
        - n-TD compute
        - derivative calculation
  - LinearTrainer
    - parameters
      - W∈R(O,A)W \in \mathbb{R}^{(O,A)}W∈R(O,A)
    - approximation
      - v(s,W)=WTsv(s,W) = W^Tsv(s,W)=WTs aW∈R(O,A)a W \in \mathbb{R}^{(O,A)}
      - output
        - a vector of A = len(action_space)
    - LinearTrainerWithFeatureConstruction
      - process_state
        - polynomial_feature
  - MLPTrainer
    - parameters
      - W1∈R(O,H),W2∈R(H,A),W^1 \in \mathbb{R}^{(O,H)}, W^2 \in \mathbb{R}^{(H,A)}, W1∈R(O,H),W2∈R(H,A),
    - approximation
      - v(s,W1,W2)=W2TW1Tsv(s,W_1,W_2) = W_2^TW_1^Tsv(s,W1,W2)=W2TW1Ts
    - Gradient clipping
  - DQNTrainer
    - ExperienceReplayMemory
    - PytorchModel
- assignment 3
  - AbstractTrainer
    - to_tensor
      - for pytorch
    - build_model
    - evaluate
    - compute_action
    - compute_values
    - compute_log_probs
    - train
      - collect and preprocess samples
        - collect_samples
          - samples
            - obs
              - episode_obs_list
            - act
              - episode_act_list
            - reward
              - episode_reward_list
            - advantage
          - sample_info
        - process_samples
      - update model
      - record train info
  - PGTrainer(AbstractTrainer)
    - CategoricalPolicy
      - Net
  - PolicyGradientWithBaselineTrainer(PGTrainer)
    - baseline
  - ActorCriticTrainer(PolicyGradientWithBaselineTrainer)
  - config
    - default_config
      - env_name="CartPole-v0",
      - max_iteration=1000,
      - max_episode_length=1000,
      - train_batch_size=1000,
      - gamma=0.99,
      - learning_rate=0.01,
      - seed=0,
      - evaluate_interval=10,
      - evaluate_num_episodes=50
    - pg_default_config
      - normalize_advantage=True
      - clip_gradient=10.0
      - hidden_units=64
    - pg_with_baseline_default_config
    - ac_default_config
      - num_critic_updates=10
      - num_critic_update_steps=10
- assignment 4
  - base_trainer
    - setup_optimizer
    - setup_rollouts
    - compute_loss
    - update
    - compute_action
    - evaluate_actions
    - compute_values
    - save_w
    - load_w
  - a2c_trainer
  - ppo_trainer
- project-pair trading
  - pair trading.ipynb
    - EV_Pairtrading_env2.py
      - 定义了env 的相关情况
      - 根据act执行相应的交易
      - portfolio.py
        - 持仓详情
        - 根据指令更改持仓
    - baseline.py
      - 定义了baseline方法的相关参数
      - 计算act函数
  - find the relation between the price changes between two stocks